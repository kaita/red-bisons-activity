{
  "name": "red-bisons-activity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RED BISONS activity management app with Google login, Sheets storage, and shared Google Calendar support.",
  "scripts": {
    "check": "node --check src/worker.js && node --check src/server.js && node --check public/app.js",
    "dev": "wrangler dev",
    "dev:cloudflare": "wrangler dev",
    "dev:node": "node src/server.js",
    "dev:demo": "DEMO_MODE=true node src/server.js",
    "deploy": "wrangler deploy",
    "smoke:local": "node scripts/smoke-local.js",
    "start": "node src/server.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
